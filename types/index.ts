export interface WebsiteProductDetailsProps {
  image: string;
  heading: string;
  subheading: string;
  border?: boolean;
}

export interface AiChallangeCardProps {
  image: string;
  heading: string;
  description: string;
}
